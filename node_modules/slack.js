var Botkit = require('botkit');
var controller = Botkit.slackbot();
var bot = controller.spawn({
    token: 'xoxb-146183821216-imH1QL9tBiWfZrcA9hskBUNk'
}).startRTM(function(err, bot, payload) {
    if (err) {
        throw new Error('Could not connect to Slack');
    }
});


var sayBot = function(message) {
    bot.say({
        channel: 'google_api_test',
        text: message,
        username: 'GoogleCalendarNotice',
        icon_url: ''
    });
};

module.exports.sayBot = sayBot;